{
  "type": "module",
  "name": "wat-server",
  "version": "1.0.0",
  "scripts": {
    "dev": "nodemon src/server.js",
    "start": "node src/server.js",
    "bundle:client": "rm -rf public && mkdir -p public && cp -r ../client/dist/* public/"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.10.0",
    "mysql2": "^3.9.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
